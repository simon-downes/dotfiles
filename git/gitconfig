[user]
	name = Simon Downes
	email = simon@simondownes.co.uk
	signingkey = /home/simon/.ssh/id_ed25519.pub

[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[branch]
	autosetuprebase = always

[color]
	ui = auto

[color "branch"]
	current = green
	local = yellow
	remote = magenta

[color "diff"]
	meta = yellow
	frag = magenta
	old = red
	new = green

[color "status"]
	added = green
	changed = yellow
	untracked = cyan

[push]
	default = current

[core]
	excludesfile = ~/.gitignore
[commit]
	gpgsign = true
[gpg]
	format = ssh
